name: algolia
on:
  push:
    branches:
      - main
    paths:
      - videos.json
jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - name: Check out code 🛎
        uses: actions/checkout@v2

      - name: Install dependencies 📦
        uses: bahmutov/npm-install@v1
